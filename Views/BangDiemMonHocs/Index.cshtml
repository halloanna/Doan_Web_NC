@model IEnumerable<DOAN_WEB_NC.Models.BangDiemMonHoc>

@{
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lop.IDLop)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MonHoc.TenMonHoc)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lop.IDLop)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MonHoc.TenMonHoc)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IDBangDiem }) |
                @Html.ActionLink("Details", "Details", new { id=item.IDBangDiem }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IDBangDiem })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
