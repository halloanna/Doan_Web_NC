@model IEnumerable<DOAN_WEB_NC.Models.MonHoc>

@{
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenMonHoc)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenMonHoc)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IDMonHoc }) |
                @Html.ActionLink("Details", "Details", new { id=item.IDMonHoc }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IDMonHoc })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
