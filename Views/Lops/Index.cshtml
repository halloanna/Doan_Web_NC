@model IEnumerable<DOAN_WEB_NC.Models.Lop>

@{
    Layout = "~/Views/Shared/_StudentLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SiSo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SLDat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TiLe)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SiSo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SLDat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TiLe)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IDLop }) |
                @Html.ActionLink("Details", "Details", new { id=item.IDLop }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IDLop })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
